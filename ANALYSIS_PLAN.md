# 대전 상권 공급 구조 분석 — 실행 계획

> 이 문서는 분석 방향 전환의 근거와 실행 절차를 정리한 작업 문서다. 1~4절과 8절은 완성 후 [REPORT.md](REPORT.md)로 옮겨 쓰는 것을 전제로 작성했다.

---

## 1. 분석 배경과 방향 전환

*(→ REPORT §1 분석 배경 및 목적)*

지금까지의 분석은 "대전의 등록 상가 수가 얼마나 변했는가"를 물었다. 그 과정에서 2024년 말 전국 단위 데이터 편입, 2025년 10월 2차 편입, 등재 지연 같은 **자료 자체의 문제**를 확인했고, 이는 신뢰 구간을 정하는 데 필요한 작업이었다. 그러나 "수록 건수가 +0.90% 늘었다"는 결과는 상권에 대해 알려주는 바가 적다.

이 분석의 목표를 **상권 분석**으로 옮긴다. 참고 모델은 서울시 우리마을가게 상권분석서비스(golmok.seoul.go.kr)다. 다만 그 서비스의 핵심 지표인 **추정매출(카드사)과 유동인구(통신사)는 대전에 공개 데이터가 존재하지 않는다.** 대전의 유동인구 자료는 유성구·서구 일부만 2023년 기준으로 공개돼 있어 5개 자치구를 일관되게 덮지 못한다.

따라서 매출·유동인구로 **수요**를 직접 측정하는 대신, **행정동 인구를 배후수요의 대리 지표로 삼아 공급 구조를 분석한다.** 상권 분석의 고전 지표인 입지계수(LQ), 다양성 지수(HHI), 인구 대비 공급 밀도는 모두 이 자료만으로 계산할 수 있다.

분석 단위도 자치구 5개에서 **행정동 82개**로 내린다. 상가정보 원본에 `행정동코드`·`행정동명`이 이미 들어 있고, 2024-03~2026-06 전 기간에 걸쳐 행정동 코드·명칭의 추가·삭제가 **0건**임을 확인했으므로([administrative_changes.csv](data/processed/administrative_changes.csv)) 구역 개편 보정 없이 균형 패널을 만들 수 있다.

기존의 데이터 품질 진단은 폐기하지 않는다. **"왜 2025-03~2026-03 구간만 쓰는가"를 정당화하는 전처리 근거**로 자리를 옮긴다.

---

## 2. 분석 질문

*(→ REPORT §2 분석 질문)*

1. 행정동별 인구 1,000명당 업소 수는 얼마나 차이 나며, 공급이 과밀한 지역과 부족한 지역은 어디인가?
2. 어느 행정동이 어떤 업종에 특화돼 있는가? (입지계수 LQ)
3. 업종 구성이 다양한 상권과 특정 업종에 쏠린 상권은 어떻게 구분되며, 어떤 유형으로 분류되는가?
4. 인구 구조(고령·청년 비율)와 업종 구성은 정합적인가? 고령 인구가 많은 동네에 실제로 의료·생활서비스가 많은가?
5. 어느 상권의 점포가 오래 살아남는가? (고정 코호트 생존율)

1~3번은 그래프로 직접 확인할 수 있고, 4~5번은 추가 해석이 필요한 질문이다.

---

## 3. 데이터

*(→ REPORT §3 데이터 설명)*

### 3.1 상가(상권)정보 — 공급 측

| 항목 | 내용 |
|---|---|
| 출처 | 소상공인시장진흥공단, [공공데이터포털](https://www.data.go.kr/data/15083033/fileData.do) |
| 기간 | 2024-03 ~ 2026-06, 분기별 파일 10개 |
| 규모 | 원본 763,839행, 고유 업소번호 114,542개 |
| 분석 단위 | **행정동 82개 × 업종 대분류 10개 × 관측월** |
| 주 분석 시점 | **2026-03** (78,607개 업소) |
| 보조 시계열 | 2025-03 ~ 2026-03 |

행정동은 8자리 코드(예: `30110515 중앙동`)로 식별하며, 전 기간 82개가 불변이다.

### 3.2 행정동 인구 — 수요 대리 지표

| 항목 | 내용 |
|---|---|
| 출처 | 행정안전부, [지역별(행정동) 성별 연령별 주민등록 인구수](https://www.data.go.kr/data/15097972/fileData.do) |
| 기준 시점 | 2026-03 (상가 데이터와 정렬) |
| 단위 | 행정동 × 성별 × 연령대 |
| 용도 | 공급 밀도의 분모, 인구 구조와 업종 구성의 정합성 검토 |

### 3.3 행정동 경계

| 항목 | 내용 |
|---|---|
| 출처 | `vuski/admdongkor` 2026-04-01(SGIS·행정안전부 경계 기반) |
| 용도 | 인터랙티브 지도의 코로플레스 레이어 |
| 조인 키 | 8자리 행정동코드 |

### 3.4 분석 데이터 규모

행정동 82개 × 대분류 10개 × 관측월 10개 = **8,200개 균형 패널 관측치**다. 주 비교 구간의 5개 시점만 보면 4,100개다. 이는 독립적인 시간 시점 수가 아니라 82개 지역과 10개 업종의 반복 관측이며, 실제 시간축은 관측월 10개다.

---

## 4. 지표 정의

*(→ REPORT §6 분석 방법)*

### 4.1 인구 대비 공급 밀도

```
공급밀도(i) = 업소수(i) / 인구(i) × 1,000
```

행정동 `i`의 인구 1,000명당 업소 수다. 절대 업소 수는 인구가 많은 동네가 무조건 높게 나오므로, 배후수요 대비 공급 수준을 보려면 이 정규화가 필요하다.

**인구 1,000명 미만 행정동**(산업단지·대학 등)은 분모가 작아 값이 폭발하므로 별도 표기하고 순위에서 제외한다.

### 4.2 입지계수 (LQ, Location Quotient)

```
LQ(i, j) = [업소수(i,j) / 업소수(i)] / [업소수(대전, j) / 업소수(대전)]
```

행정동 `i`에서 업종 `j`가 차지하는 비중을, 대전 전체에서 그 업종이 차지하는 비중으로 나눈 값이다. 지역 산업 특화도를 재는 표준 지표다.

| LQ 범위 | 해석 |
|---|---|
| ≥ 1.2 | 해당 업종에 **특화**된 상권 |
| 0.8 ~ 1.2 | 대전 평균 수준 |
| < 0.8 | 해당 업종이 **취약**한 상권 |

업소 수를 가중치로 한 LQ의 평균은 정의상 1.0이 되며, 이를 계산 검증에 쓴다.

### 4.3 업종 다양성 (허핀달-허시만 지수)

```
HHI(i) = Σ_j [업소수(i,j) / 업소수(i)]²
```

행정동 내 업종 비중의 제곱합이다. 값이 낮을수록 업종이 고르게 분포하고, 높을수록 특정 업종에 쏠려 있다.

- 대분류 10개가 완전히 균등하면 HHI = 0.1 (최대 다양성)
- 한 업종이 전부면 HHI = 1.0 (완전 쏠림)

### 4.4 상권 유형 분류

공급 밀도와 다양성을 두 축으로 4분면을 만든다.

| | 다양성 높음 | 다양성 낮음 |
|---|---|---|
| **밀도 높음** | 종합 상권 | 특화 밀집 상권 |
| **밀도 낮음** | 생활 분산형 | 저활성·단일업종 |

### 4.5 상권별 생존율

[quality_diagnostics.py](quality_diagnostics.py)의 고정 코호트 방식을 행정동 단위로 적용한다. 2024년 3월 첫 회차에 실제 수록된 업소 집합과 최초 행정동을 고정하고 회차마다 잔존 수를 센다. 나중에 편입된 업소가 집합을 키우지 못하므로 **수록 범위 확대의 영향을 줄인다.**

### 4.6 인구 구조와 업종 매칭

행정동별 고령 인구 비율(65세 이상), 청년 인구 비율(20~39세)과 업종별 LQ의 상관을 본다. 예를 들어 고령 비율이 높은 동네에서 보건의료 LQ가 실제로 높은지 확인한다. 상관은 관련성을 뜻할 뿐 인과가 아니다.

---

## 5. 실행 단계

### 🔹 사용자 작업

#### STEP 1. 행정동 인구 데이터 받기 (필수)

1. [행정안전부_지역별(행정동) 성별 연령별 주민등록 인구수](https://www.data.go.kr/data/15097972/fileData.do) 접속
2. `파일데이터` 내려받기
   - 대안: [주민등록 인구통계](https://jumin.mois.go.kr/ageStatMonth.do) → 행정동 선택 → 대전광역시 → CSV
3. **2026-03 기준** 파일을 권장한다. 없으면 가장 최근 월도 무방하다
4. `data/raw/` 폴더에 그대로 저장 (파일명 변경 불필요)

**정상 여부 확인**: 파일에 행정기관코드, `대전광역시 OO구 OO동` 형태 이름, 연령대별 인구 컬럼이 있으면 된다.

#### STEP 2. 행정동 경계 GeoJSON 받기 (지도용)

아래 중 **하나만** 하면 된다.

| 경로 | 방법 | 난이도 |
|---|---|---|
| ① 통계청 SGIS | [SGIS 오픈API](https://sgis.kostat.go.kr/) 가입 → 인증키 → 행정구역경계 API | 중 |
| ② VWorld | [VWorld](https://www.vworld.kr/) 가입 → 인증키 → 행정경계 WFS | 중 |
| ③ 공개 저장소 | 통계청 자료 기반 공개 GeoJSON (예: `vuski/admdongkor`) | 하 |

- `data/raw/` 폴더에 저장한다. 전국 파일이어도 대전만 잘라 쓴다
- ③을 고를 경우 라이선스 표기를 위해 출처 URL을 함께 남긴다

#### STEP 3. 완료 알리기

두 파일이 `data/raw/`에 들어가면 알린다.

#### STEP 4. 중간 검토 (요청 시)

- 인구 조인 결과 82개 행정동이 모두 매칭됐는지 확인
- 밀도·LQ 상위/하위 목록이 상식과 맞는지 검토 (둔산동·유성온천역 일대가 상위에 오는지)

### 🔹 분석 작업

#### Phase 1. 행정동 패널 생성

새 스크립트 `commercial_analysis.py`를 만든다. [analysis.ipynb](analysis.ipynb)는 재실행 리스크 때문에 건드리지 않는다.

기존 자산을 재사용한다.

| 재사용 대상 | 위치 | 용도 |
|---|---|---|
| `read_crosswalk()`, `build_mapping()` | [classification.py](classification.py) | 업종 837→247 정규화 |
| `normalize_records()` | [classification.py](classification.py) | 행 수를 늘리지 않는 매핑 |
| `aggregate_level(..., group_columns)` | [classification.py](classification.py) | `['period','행정동코드']`로 행정동 패널 산출 |

산출: `data/processed/dong_panel.csv`
검증: 행정동 합계 == 자치구 합계 == [daejeon_timeseries.csv](data/processed/daejeon_timeseries.csv) 총계

#### Phase 2. 인구 결합과 지표 산출

산출: `data/processed/dong_indicators.csv` (밀도·LQ·HHI·생존율·인구구조)

#### Phase 3. 시각화

정적 PNG 4개 (기존 노트북 팔레트 `#185b79`/`#bb5b28`/`#5f793e` 준수)

- `09_dong_supply_density.png` — 공급 밀도 상위·하위 15개 동
- `10_dong_lq_heatmap.png` — 행정동 × 업종 LQ 히트맵
- `11_diversity_vs_density.png` — 다양성 대비 밀도 4분면
- `12_dong_survival.png` — 행정동별 고정 코호트 잔존율

인터랙티브 지도: 기존 격자 레이어를 유지하고 **행정동 코로플레스 레이어를 토글로 추가**한다.

#### Phase 4. REPORT.md 재구성

```
1. 분석 배경 및 목적        ← 이 문서 1절
2. 분석 질문                ← 이 문서 2절
3. 데이터 설명              ← 이 문서 3절
4. 데이터 정제 및 품질 확인  ← 기존 유지
5. 신뢰 구간 판정           ← 기존 6.6절 승격
6. 분석 방법                ← 이 문서 4절
7. 분석 결과
   7.1 행정동 공급 밀도
   7.2 업종 특화도 (LQ)
   7.3 상권 다양성과 유형 분류
   7.4 인구 구조와 업종 매칭
   7.5 상권별 생존율
   7.6 인터랙티브 대시보드
8. 주요 인사이트            ← 상권 인사이트로 교체
9. 결론
10. 한계점                  ← 이 문서 8절
11. AI 사용 로그
```

---

## 6. 산출물

| 파일 | 구분 | 내용 |
|---|---|---|
| `ANALYSIS_PLAN.md` | 신규 | 이 문서 |
| `commercial_analysis.py` | 신규 | 행정동 패널·인구 결합·지표 산출 |
| `data/processed/dong_panel.csv` | 신규 | 행정동 × 업종 × 시점 패널 |
| `data/processed/dong_indicators.csv` | 신규 | 밀도·LQ·HHI·생존율 |
| `data/processed/dong_indicators_timeseries.csv` | 신규 | 행정동 지표 10개 시점 |
| `data/processed/dong_survival.csv` | 신규 | 행정동별 고정 코호트 잔존율 |
| `data/processed/daejeon_dong_boundaries.geojson` | 신규 | 82개 행정동 경계 |
| `images/09~12_*.png` | 신규 | 정적 시각화 4개 |
| `dashboard-template.html`, `dashboard.py` | 수정 | 행정동 코로플레스 레이어 |
| `REPORT.md`, `README.md` | 수정 | 주제 재구성 |
| `analysis.ipynb` | 변경 없음 | 재실행 리스크 회피 |

---

## 7. 검증 계획

1. **집계 정합성** — 행정동 합계가 자치구 합계 및 원본 행 수와 일치하는지 assert
2. **조인 완전성** — 82개 행정동이 인구 데이터와 100% 매칭되는지 확인, 미매칭 시 목록 출력 후 이름 기준 대체 매칭
3. **경계 일치** — GeoJSON의 행정동 집합이 [administrative_codes.csv](data/processed/administrative_codes.csv)의 82개와 일치하는지 assert
4. **지표 타당성** — 업소 수 가중 LQ 평균이 1.0에 수렴하는지, HHI가 0.1~1.0 범위인지 확인
5. **상식 검증** — 상위·하위 행정동 목록을 사용자가 검토
6. **시각화 확인** — PNG를 직접 열어 라벨 충돌·축 범위 점검, 대시보드는 브라우저에서 렌더링·콘솔 오류 확인

---

## 8. 한계

*(→ REPORT §10 한계점)*

- **매출과 유동인구가 없다.** 이 분석은 공급(점포) 측면만 다루며, 인구를 배후수요의 대리 지표로 쓴다. 공급이 많다고 수요가 많은 것도, 밀도가 낮다고 사업 기회가 있는 것도 아니다.
- **주간 인구를 반영하지 못한다.** 주민등록 인구는 상주인구이므로 직장인·유동인구가 많은 상업 중심지(둔산동 등)는 실제 수요보다 과소평가된다.
- **행정동 경계와 실제 상권 경계는 다르다.** 상권은 행정 경계를 넘나들며, 한 행정동 안에 성격이 다른 상권이 섞일 수 있다.
- **LQ와 HHI는 구성비 지표다.** 절대 규모를 반영하지 않으므로 업소 수가 적은 동네에서 값이 불안정하다.
- **2025-10 회차의 2차 편입**이 시계열 지표에 영향을 준다. 단면 분석(2026-03)을 주력으로 삼는 이유다.
- 인구와 상가 데이터의 기준 시점이 완전히 같지 않을 수 있으며, 그 차이는 데이터 설명에 명시한다.

---

## 9. 진행 체크리스트

### 사용자
- [x] STEP 1. 행정동 인구 데이터 저장 — `01. 인구 데이터/`에 2024-01~2026-07 31개월 전체를 받아 두어, 상가 데이터 10개 시점과 정확히 일치하는 월말 파일로 보간 없이 직접 매칭했다
- [x] STEP 2. `fetch_dong_boundaries.py`로 2026-04-01 행정동 경계를 자동 수집하고 82개 코드 검증
- [x] STEP 3. 자동 수집·검증 완료
- [x] STEP 4. 중간 검토 — 상·하위 지표, 그래프 방향, 브라우저 필터 동작 확인

### 분석
- [x] Phase 0. 실행 계획 문서 작성
- [x] Phase 1. 행정동 패널 생성 및 집계 검증
- [x] Phase 2. 인구 결합, 지표 산출 — `commercial_analysis.py`. 82개 행정동 전부 인구 매칭, 가중 LQ 평균 1.0 검증 통과, 인구 1,000명 미만 행정동 0개
- [x] Phase 3a. 정적 시각화 PNG 4개 — `dong_visuals.py` (09~12)
- [x] Phase 3b. 관측월별 행정동 공급밀도·HHI·잔존율 코로플레스
- [x] Phase 4. REPORT.md 재구성
- [x] 최종 검증 및 커밋


---

## 10. 중간 결과 (Phase 1·2·3a 완료, 2026-03 기준)

### 공급 밀도 상위/하위

인구 1,000명당 업소 수. [09_dong_supply_density.png](images/09_dong_supply_density.png)

| 상위 5 | 밀도 | 하위 5 | 밀도 |
|---|---:|---|---:|
| 동구 중앙동 | 585.3 | 서구 월평3동 | 8.6 |
| 중구 대흥동 | 147.1 | 대덕구 법1동 | 14.2 |
| 중구 문창동 | 130.2 | 중구 태평2동 | 18.6 |
| 서구 둔산1동 | 128.3 | 동구 판암2동 | 21.1 |
| 유성구 온천1동 | 123.2 | 대덕구 법2동 | 21.2 |

동구 중앙동·중구 대흥동은 원도심 상업지구로 상주인구가 적어 값이 크게 나온다 — 밀도가 곧 활성도는 아니라는 한계(§8)가 실제로 나타난 사례다.

### 인구 구조 ↔ 업종 LQ 상관 (행정동 82개 표본)

[population_category_correlation.csv](data/processed/population_category_correlation.csv)

| 업종 | 고령비율 상관 | 청년비율 상관 |
|---|---:|---:|
| 교육 서비스업 | -0.493 | -0.080 |
| 소매업 | +0.468 | -0.280 |
| 부동산업 | -0.405 | +0.225 |
| 숙박업 | +0.396 | -0.154 |
| 보건의료업 | +0.068 | -0.170 |

**보건의료업과 고령 비율의 상관은 +0.068로 거의 없다.** "고령 인구가 많으면 병의원이 많을 것"이라는 가설이 기각된다는 뜻이며, 이 자체가 §7.4의 핵심 인사이트 후보다. 반면 교육 서비스업은 고령 비율과 뚜렷한 음의 상관(-0.493)을 보인다.

### 검증 통과 항목

- 행정동 82개 × 자치구 합계가 기존 `daejeon_timeseries.csv`와 완전히 일치 (50개 시점×자치구 조합 전수 대조)
- 업소 수 가중 LQ 평균이 10개 업종 전부 1.0에 수렴
- 인구 코드(10자리, 끝 `00`)와 행정동코드(8자리)가 82/82 완전 매칭
- 10개 관측 시점 모두 정확히 일치하는 인구 월말 파일 존재 확인 (보간 불필요)

### 다음 단계

분석·시각화·리포트 재구성을 완료했다. 전체 재현 실행, 링크·문법·집계 검증 후 커밋·푸시한다.
